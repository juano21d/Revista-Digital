/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router es el comportamiento por defecto en Next.js 14+
  // No necesita configuración experimental
};

module.exports = nextConfig;
